<?xml version="1.0" encoding="UTF-8"?>
<baron-rindja-config>

    <!-- ${table.clabel}列表 -->
    <generic-grider-descriptor name="${griderName}">${griderFileName}</generic-grider-descriptor>

    <!-- ${table.clabel}编辑 -->
    <generic-editor-descriptor name="${editorName}">${editorFileName}</generic-editor-descriptor>

    <!-- ${table.clabel}查询建议器 -->
    <generic-suggestor-descriptor name="${suggestorName}">${suggestorFileName}</generic-suggestor-descriptor>
</baron-rindja-config>
